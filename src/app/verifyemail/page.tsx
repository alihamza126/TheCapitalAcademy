import VerifyEmailPage from "./verifyMail";

export const metadata = {
    title: "Verify mail",
    description: `Verify your email address to complete the registration process.`,
};

const page = () => {
    return (
        <VerifyEmailPage />
    )
}

export default page