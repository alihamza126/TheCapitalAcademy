import LoginPage from "./Signin";

export const metadata = {
  title: "Sign In",
  description: `Sign in to your account`,
};

const page = () => {
  return (
    <LoginPage />
  )
}

export default page